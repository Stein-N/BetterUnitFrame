<Ui>
    <Button name="FrameComponentTemplate" inherits="BackdropTemplate" mixin="BaseComponentMixin" virtual="true" enableMouse="true">
        <Size x="232" y="100"/>

        <Scripts>
            <OnLoad>
                self.isBuilt = false
                self.isFocused = false

                self:SetBackdrop({
                    bgFile = "",
                    edgeFile = "Interface\\AddOns\\BetterUnitFrame\\Media\\Focus-Border",
                    tile = "true", tileSize = 16, edgeSize = 16, 
                    insets = { left = -40, right = -4, top = -4, bottom = -4 } 
                })
                self:SetBackdropBorderColor(1, 1, 1, 0)
            </OnLoad>
            <OnEnter>
                if not self.isFocused then
                    self:SetBackdropBorderColor(1, 1, 1, 0.2)
                end
            </OnEnter>
            <OnLeave>
                if not self.isFocused then
                    self:SetBackdropBorderColor(1, 1, 1, 0)
                end
            </OnLeave>
            <OnMouseDown method="SetFocus"/>
        </Scripts>

        <Layers>
            <Layer level="BACKGROUND">
                <Texture parentKey="Texture" setAllPoints="true"/>
            </Layer>
        </Layers>
    </Button>

    <Button name="FrameTextureComponent" inherits="FrameComponentTemplate" mixin="FrameTextureComponentMixin" virtual="true"/>
    <Button name="HealthbarComponent" inherits="FrameComponentTemplate" mixin="HealthbarComponentMixin" virtual="true"/>
</Ui>