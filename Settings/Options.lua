Options = {
    testSlider = {
        key = "TestSlider",
        default = 50,
        enEN = {
            name = "Border Padding",
            tooltip = "Change the space between the items and the frame border."
        }
    },
    testBoolean1 = {
        key = "BUF_Test_Boolean_1",
        default = "true",
        enEN = {
            name = "Border Padding",
            tooltip = "Change the space between the items and the frame border."
        }
    },
}